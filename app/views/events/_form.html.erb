<% simple_form_for(@event) do |f| %>
  <% f.error_notification %>
  <div class="field">
    <div class="control">
      <%= f.input :title %>
      <%= f.input :title %>
      <%= f.input :title %>
      <%= f.button :submit, class: "button-is-info" %>


    </div>
  </div>
